module srff(s,r,reset,clk,q);
input s,r,clk,reset;
output reg q=0;

always@(negedge clk)
	begin
		if (reset == 0)
			q=0;
		else
			if(s==0 & r == 0)
				q = q;
			else if (s==0 & r==1)
				q=0;
			else if (s==1 & r==0)
				q=1;
			else 
				$display("Not Determined");
	end
endmodule
