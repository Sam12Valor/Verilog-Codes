module jkff(j,k,reset,clk,q);
input j,k,clk,reset;
output reg q=0;

always@(negedge clk)
	begin
		if (reset == 0)
			q=0;
		else
			if(j==0 & k == 0)
				q = q;
			else if (j==1 & k==1)
				q=~q;
			else 
				q = j;
	end
endmodule
